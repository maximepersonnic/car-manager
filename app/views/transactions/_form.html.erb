<%= simple_form_for [car, transaction] do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :date, html5: true %>
  <%= f.input :amount %>
  <%= f.button :submit_cancel, cancel: car_transactions_path(car) %>
<% end %>

